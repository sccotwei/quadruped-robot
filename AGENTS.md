# AGENTS.md

本文件适用于整个 `quadruped-robot` 仓库。

## 项目目标

在保留现有 ESP32 四足机器人基础行走能力的前提下，按 `docs/ROADMAP.md` 分阶段增加传感、姿态稳定、高级控制和 Raspberry Pi 协同能力。

## 必须遵守的 Baseline 规则

1. `quad-arduino-wifi/minikame.cpp` 中当前可工作的基础行走和舵机控制是 Phase 0 baseline。
2. 除非用户明确要求，不得修改现有运动行为、舵机参数、动作相位、幅度、偏移、周期或回中姿态。
3. 不得猜测或擅自更改 GPIO、舵机型号、舵机方向、机械结构或具体接线。
4. 尚未通过代码、硬件记录或用户确认的信息必须标记为 `UNKNOWN` 或 `TODO`。
5. 不得以清理代码为由进行无关重构。
6. 不得在未获得明确阶段任务时提前开始下一阶段开发。

## 开始工作前

1. 阅读根目录 `README.md`。
2. 阅读 `docs/HARDWARE.md`、`docs/ARCHITECTURE.md` 和 `docs/ROADMAP.md`。
3. 检查 `git status`，保留用户已有变更。
4. 明确本次工作所属 Phase 和允许修改的文件范围。

## 实施原则

- ESP32 始终负责实时运动控制。
- 计划中的 Raspberry Pi 负责高级视觉与导航；通信接口目前为 `UNKNOWN`。
- 新传感器功能应与现有运动层保持清晰边界。
- 硬件引脚、总线地址、供电方案和安装方向必须先确认，再实现。
- 优先进行小范围、可验证、可回退的变更。
- 不提交 Wi-Fi 密码、令牌或其他本地凭据。

## 验证与记录

- 修改前记录 baseline 行为和验证条件；缺失条件写为 `TODO`。
- 修改后只执行与当前任务相关的验证。
- 无法进行硬件测试时，必须明确记录为 `UNKNOWN` 或 `TODO`，不得声称已经验证。
- 调试过程、测试结果和遗留问题记录到 `docs/DEBUG_LOG.md`。
- 架构、硬件或阶段状态发生变化时同步更新对应文档。

